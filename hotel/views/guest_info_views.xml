<?xml version="1.0"?>
<odoo>
    <record id="tree_view_guest_info" model="ir.ui.view">
        <field name="model">guest.info</field>
        <field name="arch" type="xml">
            <tree string="Channel" editable="bottom">
                <field name="partner_id"/>
                <field name="guest_age"/>
                <field name="gender"/>

            </tree>
        </field>
    </record>

</odoo>